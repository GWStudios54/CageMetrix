type Env={DB:D1Database;ASSETS:Fetcher;MODEL_VERSION:string};
type Row=Record<string,any>;
const SESSION_COOKIE='cm_session';
const esc=(v:unknown)=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]!));
const cookieValue=(request:Request,name:string)=>{const raw=request.headers.get('cookie')||'';return raw.split(';').map(v=>v.trim()).find(v=>v.startsWith(`${name}=`))?.slice(name.length+1)||null;};
async function hash(value:string){const digest=await crypto.subtle.digest('SHA-256',new TextEncoder().encode(value));return Array.from(new Uint8Array(digest),b=>b.toString(16).padStart(2,'0')).join('');}
function sameOrigin(request:Request){const origin=request.headers.get('origin');return !origin||origin===new URL(request.url).origin;}
function json(data:unknown,status=200){return new Response(JSON.stringify(data),{status,headers:{'content-type':'application/json; charset=utf-8','cache-control':'no-store','x-content-type-options':'nosniff'}});}
async function adminAccount(request:Request,db:D1Database){const raw=cookieValue(request,SESSION_COOKIE);if(!raw||raw.length<30)return null;return db.prepare(`SELECT a.id,a.handle,a.display_name,a.role FROM community_sessions s JOIN community_accounts a ON a.id=s.account_id WHERE s.token_hash=? AND s.expires_at>CURRENT_TIMESTAMP AND a.role='admin' LIMIT 1`).bind(await hash(raw)).first<Row>();}

async function scopedReports(db:D1Database){const q=await db.prepare(`SELECT p.id post_id,p.scope_type,p.scope_id,p.body,p.created_at,p.deleted_at,a.handle,a.display_name,COUNT(*) report_count,GROUP_CONCAT(DISTINCT cr.reason) reasons,MIN(cr.created_at) first_report_at,e.slug event_slug FROM community_reports cr JOIN community_posts p ON p.id=cr.post_id JOIN community_accounts a ON a.id=p.account_id LEFT JOIN events e ON p.scope_type='event' AND e.id=p.scope_id WHERE cr.resolved_at IS NULL GROUP BY p.id,p.scope_type,p.scope_id,p.body,p.created_at,p.deleted_at,a.handle,a.display_name,e.slug ORDER BY report_count DESC,first_report_at ASC,p.id ASC LIMIT 100`).all<Row>();return (q.results||[]).map(r=>({post_id:Number(r.post_id),scope_type:r.scope_type,scope_id:Number(r.scope_id),body:r.body,created_at:r.created_at,hidden:!!r.deleted_at,author:{handle:r.handle,display_name:r.display_name},report_count:Number(r.report_count||0),reasons:String(r.reasons||'').split(',').filter(Boolean),target_url:r.scope_type==='fight'?`/forum/fight/${r.scope_id}`:r.event_slug?`/forum/event/${encodeURIComponent(String(r.event_slug))}`:null}));}
async function forumReports(db:D1Database){const q=await db.prepare(`SELECT p.id post_id,p.thread_id,p.body,p.created_at,p.deleted_at,t.title,t.category,a.handle,a.display_name,COUNT(*) report_count,GROUP_CONCAT(DISTINCT fr.reason) reasons,MIN(fr.created_at) first_report_at FROM forum_reports fr JOIN forum_posts p ON p.id=fr.post_id JOIN forum_threads t ON t.id=p.thread_id JOIN community_accounts a ON a.id=p.account_id WHERE fr.resolved_at IS NULL GROUP BY p.id,p.thread_id,p.body,p.created_at,p.deleted_at,t.title,t.category,a.handle,a.display_name ORDER BY report_count DESC,first_report_at ASC,p.id ASC LIMIT 100`).all<Row>();return (q.results||[]).map(r=>({post_id:Number(r.post_id),thread_id:Number(r.thread_id),thread_title:r.title,category:r.category,body:r.body,created_at:r.created_at,hidden:!!r.deleted_at,author:{handle:r.handle,display_name:r.display_name},report_count:Number(r.report_count||0),reasons:String(r.reasons||'').split(',').filter(Boolean),target_url:`/forum/thread/${r.thread_id}`}));}

export async function adminOverviewV2(request:Request,env:Env){const admin=await adminAccount(request,env.DB);if(!admin)return json({error:'admin_required'},403);const [scoped,forum,counts]=await Promise.all([scopedReports(env.DB),forumReports(env.DB),env.DB.prepare(`SELECT (SELECT COUNT(*) FROM community_accounts) accounts,(SELECT COUNT(*) FROM community_posts WHERE deleted_at IS NULL) scoped_visible,(SELECT COUNT(*) FROM community_posts WHERE deleted_at IS NOT NULL) scoped_hidden,(SELECT COUNT(*) FROM forum_threads WHERE deleted_at IS NULL) forum_threads,(SELECT COUNT(*) FROM forum_posts WHERE deleted_at IS NULL) forum_visible,(SELECT COUNT(*) FROM forum_posts WHERE deleted_at IS NOT NULL) forum_hidden,(SELECT COUNT(*) FROM community_reports WHERE resolved_at IS NULL) scoped_reports,(SELECT COUNT(*) FROM forum_reports WHERE resolved_at IS NULL) forum_reports`).first<Row>()]);return json({admin:{handle:admin.handle,display_name:admin.display_name},counts:{accounts:Number(counts?.accounts||0),visible_posts:Number(counts?.scoped_visible||0),hidden_posts:Number(counts?.scoped_hidden||0),forum_threads:Number(counts?.forum_threads||0),forum_posts:Number(counts?.forum_visible||0),forum_hidden_posts:Number(counts?.forum_hidden||0),open_reports:Number(counts?.scoped_reports||0)+Number(counts?.forum_reports||0),scoped_reports:Number(counts?.scoped_reports||0),forum_reports:Number(counts?.forum_reports||0)},reports:scoped,forum_reports:forum});}

export async function adminModerateForumPost(request:Request,env:Env,postId:string){if(!sameOrigin(request))return json({error:'Cross-origin admin actions are not allowed.'},403);const admin=await adminAccount(request,env.DB);if(!admin)return json({error:'admin_required'},403);if(!/^[1-9]\d*$/.test(postId))return json({error:'post_not_found'},404);let input:any;try{input=await request.json()}catch{return json({error:'Invalid JSON.'},400)}if(typeof input?.hidden!=='boolean')return json({error:'hidden must be true or false.'},400);const post=await env.DB.prepare('SELECT id FROM forum_posts WHERE id=? LIMIT 1').bind(Number(postId)).first<Row>();if(!post)return json({error:'post_not_found'},404);if(input.hidden)await env.DB.prepare('UPDATE forum_posts SET deleted_at=COALESCE(deleted_at,CURRENT_TIMESTAMP),updated_at=CURRENT_TIMESTAMP WHERE id=?').bind(Number(postId)).run();else await env.DB.prepare('UPDATE forum_posts SET deleted_at=NULL,updated_at=CURRENT_TIMESTAMP WHERE id=?').bind(Number(postId)).run();return json({ok:true,post_id:Number(postId),hidden:input.hidden});}
export async function adminResolveForumReports(request:Request,env:Env,postId:string){if(!sameOrigin(request))return json({error:'Cross-origin admin actions are not allowed.'},403);const admin=await adminAccount(request,env.DB);if(!admin)return json({error:'admin_required'},403);if(!/^[1-9]\d*$/.test(postId))return json({error:'post_not_found'},404);await env.DB.prepare('UPDATE forum_reports SET resolved_at=CURRENT_TIMESTAMP WHERE post_id=? AND resolved_at IS NULL').bind(Number(postId)).run();return json({ok:true,post_id:Number(postId),resolved:true});}

export async function adminDashboardPage(request:Request,env:Env){const admin=await adminAccount(request,env.DB);if(!admin){const html=`<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="robots" content="noindex"><title>Admin — CageMetrix</title><link rel="stylesheet" href="/styles.css"><link rel="icon" href="/logo.svg" type="image/svg+xml"></head><body><header class="topbar"><a class="brand" href="/"><img class="brand-mark" src="/logo.svg" width="44" height="44" alt=""><span>CageMetrix™</span></a></header><main style="max-width:760px;margin:64px auto;padding:0 24px"><p class="eyebrow">ADMIN</p><h1>Admin sign-in required</h1><p>Sign in as CageMetrix Desk, then return here.</p><a class="button primary" href="/community">Go to sign-in →</a></main></body></html>`;return new Response(html,{status:403,headers:{'content-type':'text/html; charset=utf-8','cache-control':'no-store','x-robots-tag':'noindex'}});}
const html=`<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="robots" content="noindex"><title>Admin — CageMetrix</title><link rel="stylesheet" href="/styles.css"><link rel="icon" href="/logo.svg" type="image/svg+xml"><style>.admin-shell{max-width:1180px;margin:48px auto;padding:0 24px 88px}.admin-metrics{display:grid;grid-template-columns:repeat(6,minmax(0,1fr));gap:10px;margin:24px 0 36px}.admin-metric,.admin-report{border:1px solid var(--line,#333);border-radius:16px;padding:16px;background:var(--panel,#111)}.admin-metric strong{display:block;font-size:1.7rem}.admin-queue{margin-top:36px}.admin-report{margin:12px 0}.admin-report-head,.admin-actions{display:flex;gap:10px;justify-content:space-between;align-items:center;flex-wrap:wrap}.admin-report blockquote{margin:14px 0;padding:12px 14px;border-left:3px solid currentColor;background:rgba(127,127,127,.08)}.admin-muted{opacity:.7}@media(max-width:900px){.admin-metrics{grid-template-columns:repeat(3,1fr)}}@media(max-width:560px){.admin-metrics{grid-template-columns:repeat(2,1fr)}.admin-actions .button{width:100%;text-align:center}}</style></head><body><header class="topbar"><a class="brand" href="/"><img class="brand-mark" src="/logo.svg" width="44" height="44" alt=""><span>CageMetrix™</span></a><nav><a href="/forum">Forum</a><a href="/community">Community</a><a href="/u/${encodeURIComponent(String(admin.handle))}">@${esc(admin.handle)}</a></nav></header><main class="admin-shell"><p class="eyebrow">CAGEMETRIX DESK</p><h1>Admin panel</h1><p class="admin-muted">Signed in as ${esc(admin.display_name)} · @${esc(admin.handle)}</p><div id="admin-root"><p>Loading moderation queues…</p></div></main><script>const root=document.getElementById('admin-root'),e=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));async function api(url,opts){const r=await fetch(url,{credentials:'same-origin',...opts}),j=await r.json().catch(()=>({}));if(!r.ok)throw new Error(j.error||('HTTP '+r.status));return j}function queue(title,eyebrow,items,kind){if(!items.length)return '<section class="admin-queue"><p class="eyebrow">'+eyebrow+'</p><h2>'+title+'</h2><p class="admin-muted">No open reports.</p></section>';return '<section class="admin-queue"><p class="eyebrow">'+eyebrow+'</p><h2>'+title+'</h2>'+items.map(r=>'<article class="admin-report"><div class="admin-report-head"><div><strong>#'+r.post_id+' · @'+e(r.author.handle)+'</strong><div class="admin-muted">'+(kind==='forum'?e(r.thread_title):e(r.scope_type))+' · '+r.report_count+' report'+(r.report_count===1?'':'s')+(r.hidden?' · hidden':'')+'</div></div>'+(r.target_url?'<a class="button secondary" href="'+e(r.target_url)+'">Open context ↗</a>':'')+'</div><blockquote>'+e(r.body)+'</blockquote><p><strong>Reasons:</strong> '+e((r.reasons||[]).join(', ')||'other')+'</p><div class="admin-actions"><button class="button secondary" data-kind="'+kind+'" data-hide="'+r.post_id+'" data-next="'+(!r.hidden)+'">'+(r.hidden?'Restore post':'Hide post')+'</button><button class="button primary" data-kind="'+kind+'" data-resolve="'+r.post_id+'">Resolve reports</button></div></article>').join('')+'</section>'}async function load(){try{const d=await api('/api/admin');root.innerHTML='<div class="admin-metrics"><div class="admin-metric"><strong>'+d.counts.accounts+'</strong><span>accounts</span></div><div class="admin-metric"><strong>'+d.counts.visible_posts+'</strong><span>fight/event posts</span></div><div class="admin-metric"><strong>'+d.counts.forum_threads+'</strong><span>forum threads</span></div><div class="admin-metric"><strong>'+d.counts.forum_posts+'</strong><span>forum posts</span></div><div class="admin-metric"><strong>'+d.counts.open_reports+'</strong><span>open reports</span></div><div class="admin-metric"><strong>'+(d.counts.hidden_posts+d.counts.forum_hidden_posts)+'</strong><span>hidden posts</span></div></div>'+queue('Fight & event reports','FIGHT NIGHT',d.reports||[],'scoped')+queue('Forum reports','FORUM',d.forum_reports||[],'forum');root.querySelectorAll('[data-hide]').forEach(b=>b.onclick=async()=>{b.disabled=true;try{const base=b.dataset.kind==='forum'?'/api/admin/forum/posts/':'/api/admin/posts/';await api(base+b.dataset.hide,{method:'PUT',headers:{'content-type':'application/json'},body:JSON.stringify({hidden:b.dataset.next==='true'})});await load()}catch(err){alert(err.message);b.disabled=false}});root.querySelectorAll('[data-resolve]').forEach(b=>b.onclick=async()=>{b.disabled=true;try{const base=b.dataset.kind==='forum'?'/api/admin/forum/reports/':'/api/admin/reports/';await api(base+b.dataset.resolve,{method:'PUT',headers:{'content-type':'application/json'},body:JSON.stringify({resolved:true})});await load()}catch(err){alert(err.message);b.disabled=false}})}catch(err){root.innerHTML='<p>'+e(err.message)+'</p>'}}load();</script></body></html>`;return new Response(html,{headers:{'content-type':'text/html; charset=utf-8','cache-control':'no-store','x-robots-tag':'noindex'}});}
