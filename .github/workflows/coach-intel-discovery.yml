name: Discover public MMA coach-change intelligence

on:
  schedule:
    - cron: '45 12 * * *'
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - 'migrations/0048_coach_intelligence.sql'
      - 'scripts/discover-coach-intel.mjs'
      - 'scripts/lib/coach-intel-discovery.mjs'
      - '.github/workflows/coach-intel-discovery.yml'

# Coach discovery is idempotent and independent from Worker releases. Keep it out of the production
# deploy lane so a UI deploy cannot discard a valid source-discovery run from the same commit (same
# reasoning as contract/camp/injury/anti-doping discovery).
concurrency:
  group: mmascouts-coach-intel
  cancel-in-progress: false

jobs:
  discover:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: '24'
      - run: npm ci
      - name: Apply coach intelligence schema
        run: npx wrangler d1 migrations apply cagemetrix --remote
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
      - name: Discover source-backed coach-change leads
        run: node scripts/discover-coach-intel.mjs --remote
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: coach-intel-discovery
          path: .cache/coach-intel/
          if-no-files-found: ignore
          retention-days: 14
